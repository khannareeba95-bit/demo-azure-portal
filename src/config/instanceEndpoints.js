import { ENDPOINTS } from "../config/endpoints";

export const instanceEndpoints = {
  genaigetStatusURL:  ENDPOINTS.GENAI_STATUS,
  ocrgetStatusURL:    ENDPOINTS.GENAI_STATUS_OCR,
  nttgetStatusURL:    ENDPOINTS.GENAI_STATUS,
  genaiStartURL:      ENDPOINTS.GENAI_START,
  genaiStopURL:       ENDPOINTS.GENAI_STOP,
  ocrStartURL:        ENDPOINTS.OCR_API,
  ocrStopURL:         ENDPOINTS.OCR_API,
  nttStartURL:        ENDPOINTS.GENAI_START,
  nttStopURL:         ENDPOINTS.GENAI_STOP,
};
